namespace finlang.test;

public class array_Test
{
    //[Fact]
    //public void First()
    //{
    //    arr8<u8> counts = mem.stack(new arr8<u8>(new u8[] { 0, 1, 2, 3, 4 }));

    //   { 
    //        u8? c = counts.try_get(1000);
    //        u8 b = c.Value; // throws exception in C# if c doesn't have a value

    //        if (c.HasValue)
    //        {

    //        }

    //        optional<u8>? d = counts.try_get(1000);

    //        if (d != null)
    //        {
    //            u8 dd = d;
    //        }
    //    }

    //    {

    //        u8? d = null;

    //        arr8<object> objs = mem.stack(new arr8<object>(new object[] { 0, 1, 2, 3, 4 }));
    //        var x = objs.try_get(0);
    //        x.ToString();
    //    }

    //}
}
