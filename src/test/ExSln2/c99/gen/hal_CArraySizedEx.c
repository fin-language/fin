// AUTOGENERATED FILE. Do not modify this file manually, it will be overwritten.
// finlang generated this file for C# `hal.CArraySizedEx` type.
// Source file: `LedBlinker\test_stuff\CArraySizedEx.cs` (relative to C# solution).


#include "hal_CArraySizedEx.h"
#include <string.h>



void hal_CArraySizedEx_ctor(hal_CArraySizedEx * self)
{
    memset(self, 0, sizeof(*self));
    finlang_c_array_sized_ctor(&self->data, 5);

}

uint16_t hal_CArraySizedEx_sum(hal_CArraySizedEx * self)
{
    uint16_t sum = 0;
    for (uint8_t i = 0; i < (sizeof(self->data)/sizeof(self->data[0])); i++)
    {
        sum += self->data[i];
    }
    return sum;
}

uint16_t hal_CArraySizedEx_sum2(hal_CArraySizedEx * self)
{
    return hal_CArraySizedEx_sum_c_array(self->data, (uint8_t)(sizeof(self->data)/sizeof(self->data[0])));
}

uint8_t hal_CArraySizedEx_get_element(hal_CArraySizedEx * self, uint8_t index)
{
    return self->data[index];
}

uint16_t hal_CArraySizedEx_sum_c_array(uint8_t * arr, uint8_t length)
{
    uint16_t sum = 0;
    for (uint8_t i = 0; i < length; i++)
    {
        sum += arr[i];
    }
    return sum;
}
